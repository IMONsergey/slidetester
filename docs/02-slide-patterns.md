# Slide Patterns

Use reference matching from existing Figma frames.

The addendum has priority: every generated slide must declare which existing Figma frame(s) it borrows from and what exactly is reused.

Initial known patterns:

1. `hero_cover_glow`
   - source: `25:6981`
   - role: cover / hero title

2. `dense_market_chart`
   - source: `25:7247`
   - role: dense analytics / chart slide

3. `three_pillar_outcome_architecture`
   - source: `25:10717`
   - role: three-pillar strategic outcome slide

4. `operating_model_architecture`
   - source: `25:10434`, `25:10876`
   - role: structured operating model / system / loop slide

Codex must expand this into `output/pattern-library.json`.
