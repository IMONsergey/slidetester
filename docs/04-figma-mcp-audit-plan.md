# Figma MCP Audit Plan

Do not request full metadata for page `0:1`.

Use compact index extraction, selected screenshots, selected design_context, and local JSON cache.

Avoid full-page recursive audits. Work in batches and promote only validated reference frames into the local pattern library.

Required outputs:

- `output/pattern-library.json`
- `output/pilot-reference-map.json`
